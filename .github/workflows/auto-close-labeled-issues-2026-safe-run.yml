name: Auto Close Labeled Issues
on:
  issues:
    types: [labeled, opened, reopened]
  workflow_dispatch:
jobs:
  close-issues:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Close completed and wontfix issues
        uses: actions/github-script@v7
        with:
          script: |
            const labelsToClose = ['completed', 'wontfix'];
            const issues = context.payload.issue ? [context.payload.issue] : await github.paginate(github.rest.issues.listForRepo, {owner: context.repo.owner, repo: context.repo.repo, state: 'open'});
            for (const issue of issues) {
              const labelNames = issue.labels.map(l => typeof l === 'string' ? l : l.name);
              if (labelNames.some(l => labelsToClose.includes(l))) {
                await github.rest.issues.update({owner: context.repo.owner, repo: context.repo.repo, issue_number: issue.number, state: 'closed'});
              }
            }
